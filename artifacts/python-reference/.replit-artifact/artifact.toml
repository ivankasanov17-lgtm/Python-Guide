kind = "web"
previewPath = "/"
title = "Python Reference"
version = "1.0.0"
id = "artifacts/python-reference"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18827

[services.development]
run = "pnpm --filter @workspace/python-reference run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/python-reference", "run", "build" ]
publicDir = "artifacts/python-reference/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18827"
BASE_PATH = "/"
